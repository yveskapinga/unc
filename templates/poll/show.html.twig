{% extends 'base.html.twig' %}

{% block title %}Poll{% endblock %}

{% block body %}
    <h1>Poll</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poll.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ poll.question }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_poll_index') }}">back to list</a>

    <a href="{{ path('app_poll_edit', {'id': poll.id}) }}">edit</a>

    {{ include('poll/_delete_form.html.twig') }}
{% endblock %}
