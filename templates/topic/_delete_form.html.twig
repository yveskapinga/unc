<form method="post" action="{{ path('app_topic_delete', {'id': id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ id) }}">
    <button class="btn btn-link p-0 m-0 align-baseline" style="color: inherit; text-decoration: none;">
        {#<i class="fas fa-trash-alt"></i>#}
    </button>
</form>
