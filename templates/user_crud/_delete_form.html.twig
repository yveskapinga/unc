<form method="post" action="{{ path('app_user_crud_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes vous sûre de vouloir supprimer cette demande?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger">{{ button_label|default('Delete') }}</button>
</form>
